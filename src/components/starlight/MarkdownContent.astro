---
import StarlightMarkdownContent from '@astrojs/starlight/components/MarkdownContent.astro';
import type { Props } from '@astrojs/starlight/props';
import ExampleImages from '../ExampleImages.astro';
import PluginContentHeader from '../PluginContentHeader.astro';
---

{
  Astro.props.entry.data.is_plugin ? (
    <PluginContentHeader {...Astro.props} />
  ) : null
}

<StarlightMarkdownContent {...Astro.props}><slot /></StarlightMarkdownContent>

<ExampleImages />
