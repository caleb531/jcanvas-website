---
import path from 'node:path';
const plugin_id = path.basename(Astro.props.entry.slug);
---

<a
  href={`https://cdnjs.cloudflare.com/ajax/libs/jcanvas/22.1.3/esm/jcanvas-${plugin_id}.min.js`}
  class="button download"
>
  Download (cdnjs)
</a>
